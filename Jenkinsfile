pipeline {

    agent {
        docker {

        }
    }

    environment {
        project_name = 'uat/authentication'
        outputPath = 'authentication'
    }

    stages {

        stage('Install') {
            steps {
                sh 'pwd'

            }
        }

        stage('Build') {
            steps {
                sh 'pwd'

            }
        }

    }
}
